import { WebLinkDirective } from './web-link.directive';

describe('WebLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new WebLinkDirective();
    expect(directive).toBeTruthy();
  });
});
